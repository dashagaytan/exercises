const form = document['travelForm'];

form.addEventListener("submit", (event)=>{
    event.preventDefault()

    const checkedDiet = []
    for( const i=0; i< checkedDiet.length; i++){
        checkedDiet.push(checkedDiet[i].value);
    }

    
})